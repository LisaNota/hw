void reverseString(char* s, int sSize){
    int tmp;
    for (int i = 0; i <= floor((sSize-2)/2); i++) {
        tmp = s[i];
        s[i] = s[sSize - 1 - i];
        s[sSize - 1 - i] = tmp;
    }
    return s;
}
